name: weatherapp
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  dio: ^3.0.10
  flutter_dotenv: ^2.1.0
  geolocator: ^6.1.5
  get_it: ^5.0.1
  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  provider: ^4.3.2+2

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  test: ^1.15.4

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/
